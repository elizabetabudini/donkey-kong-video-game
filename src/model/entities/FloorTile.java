package model.entities;

import java.awt.Dimension;

public class FloorTile extends EntityImpl implements Environment {

    public FloorTile(Double x, Double y, Dimension dim) {
        super(x, y, dim);
    }

}
